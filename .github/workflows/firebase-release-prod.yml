# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting PROD

#on: pull_request
on:
  release:
    types:
      - released
      - prereleased
  workflow_dispatch:

permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    #if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    env:
      VITE_FIREBASE_APIKEY: ${{ secrets.FIREBASE_API_KEY }}
      VITE_FIREBASE_PROJECTID: ${{ secrets.FIREBASE_PROJECTID }}
      VITE_IMPRINT_NAME: ${{ secrets.IMPRINT_NAME}}
      VITE_IMPRINT_ADDRESS: ${{ secrets.IMPRINT_ADDRESS}}
      VITE_IMPRINT_ADDRESS2: ${{ secrets.IMPRINT_ADDRESS2}}
      VITE_IMPRINT_EMAIL: ${{ secrets.IMPRINT_EMAIL}}
      VITE_SOFTWARE_VERSION: ${{ github.event.release.tag_name }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run lint && npm run build
        working-directory: ./voter_app
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VOTER_61D81 }}
          projectId: voter-61d81
          channelId: live
